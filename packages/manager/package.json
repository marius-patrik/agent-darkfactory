{
  "name": "@marius-patrik/andromeda-manager",
  "version": "0.1.0",
  "description": "Agent OS manager package implementing the `agents` CLI and canonical state, provider, session, package, and capability workflows.",
  "type": "module",
  "bin": {
    "agents": "./src/cli.ts"
  },
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test test/*.test.ts",
    "ci": "bun run check && bun run test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@bufbuild/protobuf": "2.12.1",
    "@connectrpc/connect": "2.1.2"
  },
  "engines": {
    "bun": "1.3.14"
  },
  "license": "MIT"
}
