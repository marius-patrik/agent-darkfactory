# terminal

Client only: the terminal that connects to the mainframe.

Renders and forwards intent. No business logic — anything the terminal needs to know must be reachable through the mainframe API.
