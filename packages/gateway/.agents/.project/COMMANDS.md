# Commands

- `python -m uv sync --frozen`
- `python -m uv run ruff check llm_gateway tests`
- `python -m uv run mypy llm_gateway`
- `python -m uv run pytest -q`
- `AGENTS_HOME=/absolute/disposable/.agents AGENTS_USER_HOME=/absolute/disposable AGENTS_ROOT=/absolute/Andromeda python -m uv run python scripts/packaging_smoke.py`
- `python -m uv build`
